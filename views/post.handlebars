<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_travel_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span role="img" aria-label="destination">🗺️</span>
    Destination: <span class="destination-details">{{destination_map}}</span>
  </p>
 
  <p>
    <button class="btn btn-primary">Check out this Travel Post!</button>
  </p>
</div>
