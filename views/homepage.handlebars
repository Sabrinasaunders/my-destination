<!-- Loop over each object in the TravelPosts array -->
{{#each TravelPosts as |TravelPost| }}
<div class="row mb-4 TravelPost">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/TravelPost/{{TravelPost.id}}">{{TravelPost.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="money">💲</span>
      <!-- Pass needed_funding value to the helper function -->
      <span class="dollar-amount">{{format_amount TravelPost.needed_funding}}</span>
      needed!
    </p>
    <p>Created by {{TravelPost.user.name}} on {{format_date TravelPost.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{TravelPost.description}}
    </p>
  </div>
</div>
{{/each}}
